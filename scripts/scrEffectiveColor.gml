//scrEffectiveColor(color);
// returns the effective color; used for calculations
var color = argument0;
if browned { color = color_BROWN }
if color == color_GLITCH { color = glitchMimic }
return color;
